# type declarations
typ int_arr : arr[10] integer;
typ bool : logical;

# variable declarations
var x : bool;

# function declarations
fun fn(x : int, a : bool) : integer = (
	{ if x > 10 & a == true then 
	(
		printf ( 'danes je lep dan' ),
		'se en expression v if stavku'
	)},
	{ x = 10 * 10 },
	(((((((x <= (!10 * --10))))))))
);

fun main (argv : arr[10] string, argc : int) : integer = (
	printf ('prvi argument tabele = ' + argv[0]),
	return ( EXIT_SUCCESS )
);


# type definition
typ char : integer